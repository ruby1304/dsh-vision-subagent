---
name: Bug report
about: Something broke — vision_agent, the paste bridge, or vision_image_fetch
labels: bug
---

**What happened** (and what you expected instead)

**Which path** — paste bridge / vision_agent tool / vision_image_fetch / settings

**Environment**
- dsh version (`dsh --version`):
- plugin version:
- vision route (provider/model, e.g. kimi-coding/k3):
- OS / browser (for web UI issues):

**Error text** — the exact VISION_* code and message, or the console/server log excerpt (never paste API keys)

**Repro** — steps or a minimal image/message that triggers it
